#!/usr/bin/env bash
# Launch Grok Build as the Cybergrok session agent with this plugin and toolchain on PATH.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export CYBERGROK_DIR="${SCRIPT_DIR}"
export CYBERGROK_ROOT="${SCRIPT_DIR}"
export PATH="${SCRIPT_DIR}/tools/bin:${SCRIPT_DIR}/bin:${SCRIPT_DIR}/venv/bin:${PATH}"
umask 0002

if [ -f "${SCRIPT_DIR}/.env" ]; then
  set -a
  # shellcheck disable=SC1091
  source "${SCRIPT_DIR}/.env"
  set +a
fi

if [ -f "${SCRIPT_DIR}/venv/bin/activate" ]; then
  # shellcheck disable=SC1091
  source "${SCRIPT_DIR}/venv/bin/activate"
fi

if ! command -v grok >/dev/null 2>&1; then
  echo "Error: Grok Build CLI ('grok') not found on PATH."
  echo "Install Grok Build, then re-run: ./cybergrok"
  echo "Plugin files are ready: grok plugin install \"${SCRIPT_DIR}\" --trust"
  exit 1
fi

AGENT_FILE="${SCRIPT_DIR}/.grok/agents/cybergrok.md"
if [ ! -f "${AGENT_FILE}" ]; then
  AGENT_FILE="${SCRIPT_DIR}/agents/cybergrok.md"
fi
export GROK_AGENT="${GROK_AGENT:-${AGENT_FILE}}"

GROK_ARGS=()
# Prefer the session agent definition (project .grok/agents/ or plugin agents/).
GROK_ARGS+=(--agent "${GROK_AGENT}")
# Folder trust unlocks project plugins, hooks, and repo-local MCP.
if grok --help 2>/dev/null | grep -q -- '--trust'; then
  GROK_ARGS+=(--trust)
fi

exec grok "${GROK_ARGS[@]}" "$@"
